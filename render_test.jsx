import React from 'react';
import { render } from '@testing-library/react';
import { ReferralsPage } from './src/components/member/ReferralsPage.jsx';

// Jest configuration ensures tests can run, but we don't need a browser to verify
// that the component structure is rendered without crashing.
